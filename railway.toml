[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn fegg_jaay.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/login/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
